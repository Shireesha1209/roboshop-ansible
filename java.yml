- name: Install Maven
  ansible.builtin.yum:
    name: maven
    state: installed

- name: App Pre-requisites
  ansible.builtin.import_tasks: app-preq.yml

- name: Download Maven Dependencies
  ansible.builtin.shell: mev clean package ; mv target/{{component}}-1.0.jar {{component}}.jar
  args:
    chdir: /app

- name: Schema SetUp
  ansible.builtin.import_tasks: mysql-schema.yml
  when: schema is defined

- name: SystemD SetUp
  ansible.builtin.import_tasks: systemd-setup.yml


#  func_schema_setup
#
#  func_systemd


